import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 20,
  },
  titleContainer: {
    alignItems: "center",
    marginTop: 40,
    marginBottom: 30,
  },
  contentContainer: {
    gap: 16,
    alignItems: "center",
  },
});
